# 班主任工作台 · 功能复查问题列表

- 复查日期：2026-08-25
- 复查方式：代码走查（app.js / data.js / store.js / css）+ 全量回归测试（当前 203 项全部通过）
- 优先级：**高** = 数据正确性或明显故障；**中** = 功能边界 / 一致性；**低** = 体验优化

## 问题汇总

| # | 模块 | 问题 | 优先级 |
|---|------|------|--------|
| 1 | 花名册导入 | 替换花名册后，成绩、点名状态按旧学生 ID 存储，会与新名单失联 | 高 |
| 2 | 抽屉列表 | 考勤/纪律/巡查/家校/成长/班会/活动列表删除无确认，误删难恢复 | 高 |
| 3 | 花名册 | 导出 CSV/Excel 与导入模板都没有「学籍号」列，导出再导入会丢学籍号 | 高 |
| 4 | 成绩分析 | 「总分」对缺考科目按已考科目求和，缺考学生总分口径不一致 | 高 |
| 5 | 全局 | 删除/改名学生后，作业未交名单、值日排班、班会/活动记录中的姓名是文本快照，不会同步 | 中 |
| 6 | 小组 | 小组数写死 1-8（表单下拉、座次表/花名册/成绩筛选），导入小组 >8 或自定义会看不到 | 中 |
| 7 | 值日表 | 值日名单固定每天 6 人，无法 5/7 人值班；没有清空/删除某周排班 | 中 |
| 8 | 课程表 | 科目下拉为固定列表；导入 CSV 中出现不在列表内的科目时，编辑该格无法保留原科目 | 中 |
| 9 | 座次表 | 排/列上限固定（1-12 排 × 1-16 列），超规模教室无法表达 | 低 |
| 10 | 成绩分析 | 考试名称可重复；导入成绩会新建同名考试 | 低 |
| 11 | 初始化 | 「空白开始」后考勤 total=0，之后导入花名册不会自动刷新，直到下次点名 | 低 |
| 12 | 班级活动 | 照片引用 `assets/photos/` 文件名，文件缺失时显示破图（应用内不做上传，按你要求） | 低 |
| 13 | 表单 | 各记录日期为自由文本，无日期选择器/格式校验（09-01 与 2026-09-01 混用） | 低 |
| 14 | 成绩录入 | 分数输入允许小数，保存仅做 0-100 截断，未取整/提示 | 低 |
| 15 | 考勤 | 手动在名单里添加非花名册名字后，下次点名会按花名册重建而清掉，用户可能不理解 | 低 |
| 16 | 一致性 | 删除确认不一致：花名册/班委/备份/考试有确认，抽屉列表删除没有 | 低 |
| 17 | 学生表单 | 学籍号无唯一性校验，可重复 | 低 |
| 18 | 成绩排名 | 并列分数名次为连续编号（非并列名次），进退步按名次差计算 | 低 |
| 19 | 今日课程 | 「我的课」高亮依赖教师姓名与班级设置完全一致，不一致时不高亮 | 低 |
| 20 | 顶栏 | 按钮较多（设置/导出/导入/使用手册），窄屏可能拥挤 | 低 |

## 详细说明与建议

### 高优先级

**1. 替换花名册后，成绩 / 点名状态失联**
导入中心「花名册」导入会清空并重建学生（新分配 id），而成绩考试分数、早读点名 marks 均按旧学生 id 存储。替换后成绩分析排名空白、点名状态丢失。
建议：导入时按「姓名 / 学籍号」映射保留原 id；无法匹配的再新建；或至少弹窗提示「成绩、考勤将按新名单重新关联」。当前测试数据（花名册-测试数据）与成绩测试数据已做到姓名一致，可借此验证映射方案。

**2. 抽屉内列表删除无确认**
考勤迟到/请假/缺勤、课堂纪律、巡查记录、家校沟通、成长记录、班会/活动等列表的删除（编辑模式下 ×）直接生效，误删无法恢复；与花名册、班委考核、备份、考试等有确认的删除不一致。
建议：`deleteDrawerItem` 统一加确认弹窗（可批量统一，不影响现有添加/编辑）。

**3. 花名册导出丢失学籍号**
导出 CSV/Excel 列：姓名、性别、小组、排、列、班委职务、家长姓名、联系电话——没有学籍号；导入模板也没有该列。导出后再导入，学籍号按序号重新生成。
建议：导出追加「学籍号」列（放姓名后或末列均可，导入解析忽略未知列，兼容旧文件）。

**4. 成绩总分口径**
`totalOf` 只对已考科目求和，缺考学生按较少科目合计，与满分不一致（例如少考 1 科也能拿到较高总分）。
建议：缺考按 0 分计并在档案标注「缺考 X 科」，或引入“总分/参考科目数”双口径，并在排名表注明。

### 中优先级

**5. 姓名快照不同步**：作业未交名单、值日排班、班会/活动记录里的学生姓名是文本。学生改名/删除后这些地方显示旧名或无效名。建议在删除/改名时提示关联数据不受影响，或逐步改为按 id 引用。

**6. 小组数写死 1-8**：学生表单小组下拉、座次表/花名册/成绩的小组筛选均为 1-8。若班级小组数不同或导入文件小组 >8，这些学生无法在小组视图筛选、座位无组色。建议小组数量可配置（默认 8），筛选和下拉随之生成。

**7. 值日固定 6 人**：名单调整强制选满 6 人，无法按实际安排 5/7 人；也无清空/删除某周排班。建议每天人数可配置，并支持清空某周后重新排班。

**8. 课程表科目固定列表**：单元格编辑的下拉为固定 `SUBJECTS`；导入 CSV 出现列表外科目（如「研学」）后，点该格编辑会因无对应选项而无法保留原科目。建议科目列表可配置，或把已导入科目自动加入可选项。

### 低优先级 / 优化建议

- 座次表排/列上限、成绩考试重名、考勤 total 联动、照片破图占位、日期格式校验、分数小数取整、学籍号唯一性、并列名次、教师姓名精确匹配、窄屏顶栏收纳等，见汇总表 9-20 项，均为体验级优化，可按需分批处理。

## 已确认按你的要求保留 / 不做

- 「家长联系方式」独立页面已移除，改为花名册电话一键复制。
- 使用手册与 README 已移除部署相关内容。
- 班会/活动照片不做应用内上传，照片放 `assets/photos/` 本地文件夹展示。

## 建议处理顺序

1. 先修 1-4（数据正确性）：花名册替换联动、删除确认、导出补学籍号、总分口径。
2. 再处理 5-8（边界与一致性）：姓名快照提示、小组可配置、值日人数可配置、课程表科目扩展。
3. 其余低优先级按使用反馈分批优化。
